The range instruction matches against the input at the engine's current input
offset. If this offset is bigger than or equal to the input's size, or if
the match fails, the FAIL state of the engine of the engine will be invoked.
